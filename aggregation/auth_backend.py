#Authetnication backend for python. Very simple

#simply adds a user and password, leverages bcrypt to hash the password
def add_user(username, password):


#authenticates a user and returns a boolean that tells us if the user exists in the authentication system
def auth_user(username, password):
